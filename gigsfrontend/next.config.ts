import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
        domains: ['s.gravatar.com', 'cdn.auth0.com', 'designtemplate.tech', ],  // Add any other domains here
    }
};

export default nextConfig;
